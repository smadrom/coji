# API Verification — External API Fact-Check

**Date:** 2026-06-08  
**Author:** coji maintainers
**Purpose:** Mandatory P0 gate. Verify every external-API claim in the plan before any provider is coded. Each fact is marked CONFIRMED / CHANGED / UNVERIFIED with a source URL.

---

## 1. Google Gemini — Image Generation

### 1.1 Model IDs

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `gemini-3.1-flash-image` — stable model for character-consistent generation via `generateContent` | **CONFIRMED** | Documented as "Nano Banana 2", designated **Stable**, supports "Up to 4 images of characters to maintain character consistency." No `-preview` suffix needed. | https://ai.google.dev/gemini-api/docs/models |
| `imagen-4.0-generate-001` — predict-based model, `sampleCount` 1–4, `personGeneration:allow_adult` | **CONFIRMED** | Model ID `imagen-4.0-generate-001` exists and uses the `:predict` endpoint at `https://generativelanguage.googleapis.com/v1beta/models/imagen-4.0-generate-001:predict`. `sampleCount` range 1–4 confirmed. `personGeneration` options: `allow_adult` (default), `allow_all`, `dont_allow`. Note: `allow_all` is not permitted in EU/UK/CH/MENA regions. | https://ai.google.dev/gemini-api/docs/imagen |

**Additional model IDs found (not in plan, for reference):**
- `imagen-4.0-ultra-generate-001` — ultra quality variant
- `imagen-4.0-fast-generate-001` — fast variant
- `gemini-3-pro-image` — "Nano Banana Pro", stable, professional asset production
- `gemini-2.5-flash-image` — "Nano Banana", stable, speed/efficiency

### 1.2 SDK — `@google/genai`

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `@google/genai` v2.8.0 install | **CONFIRMED** | Latest version is **2.8.0**, published ~June 6, 2026. Install: `npm i @google/genai` (or `bun add @google/genai`). Supports both Google AI Studio and Gemini Enterprise Agent Platform (Vertex AI). By default uses beta API endpoints for preview features. | https://www.npmjs.com/package/@google/genai |

### 1.3 `generateContent` parameters

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `generateContent` with `responseModalities: ["TEXT", "IMAGE"]` | **CONFIRMED** | Pattern confirmed: `config: { responseModalities: ["TEXT", "IMAGE"] }`. When set to `["IMAGE"]` only (omitting TEXT), the model generates at higher resolution. | https://ai.google.dev/gemini-api/docs/image-generation |
| Character consistency: frame-1 fed back as `inline_data` reference for calls 2–4 | **CONFIRMED** | `gemini-3.1-flash-image` supports "Up to 4 images of characters to maintain character consistency" and "Up to 14 reference images" with "high-fidelity" output. The `inline_data` mechanism for feeding reference images is the documented approach via `generateContent` multi-turn. | https://ai.google.dev/gemini-api/docs/image-generation |
| `candidateCount > 1` → HTTP 400 for image generation | **CONFIRMED** | Documentation shows only single candidate responses for image output. No `candidateCount > 1` support is documented or exhibited. Forum thread confirms this restriction. | https://discuss.ai.google.dev/t/gemini-3-1-flash-image-preview-only-generate-1k-image/142496 |
| `personGeneration: "allow_adult"` for `gemini-3.1-flash-image` | **UNVERIFIED** | `personGeneration` as a parameter is confirmed for Imagen 4 (`:predict` endpoint). For `gemini-3.1-flash-image` (`generateContent` endpoint) the `personGeneration` parameter is **not explicitly documented** in the image-generation reference fetched. It may be supported via `generationConfig` but was not confirmed in official docs. **Recommend:** verify via the full SDK reference or test with the Noop path before P1 coding; do not assume parity with Imagen 4. | https://ai.google.dev/gemini-api/docs/image-generation |

### 1.4 Base64 inline output

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| Images returned as base64 inline data | **CONFIRMED** | For Imagen 4: `generatedImage.image.imageBytes` is base64-encoded data, converted to a Buffer and written to file. Gemini-native image generation also returns inline base64 via response parts. | https://ai.google.dev/gemini-api/docs/imagen |

### 1.5 SynthID watermark

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| SynthID watermark — non-removable, disclosure note required | **CONFIRMED** | All images generated by `gemini-3.1-flash-image` include "a SynthID watermark" for identification. Non-removability is implied by Google's responsible-AI policy but not explicitly stated in per-page text found. **Recommend:** add disclosure note in project metadata as planned; treat as non-removable per Google policy. | https://ai.google.dev/gemini-api/docs/image-generation |

---

## 2. HeyGen — Video Generation

### 2.1 API version and base URL

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| v3 is the current API, base `https://api.heygen.com/v3` | **CONFIRMED** | v3 is the active platform with all new features and performance improvements. Legacy v1/v2 remain operational with support through **October 31, 2026**. New capabilities (Avatar IV image-to-video, Video Agent, etc.) are v3-exclusive. | https://developers.heygen.com/docs/quick-start |
| Authentication header: `X-Api-Key` | **CONFIRMED** | Header: `X-Api-Key: <your-key>`. Also accepted as `x-api-key` (lowercase) per curl examples in asset upload docs. | https://developers.heygen.com/reference/upload-asset |

### 2.2 POST /v3/assets — upload

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `POST /v3/assets` — multipart upload → returns `asset_id` | **CONFIRMED** | Endpoint: `POST https://api.heygen.com/v3/assets`. Content-Type: `multipart/form-data`. Single form field: `file` (binary). Max 32 MB. Supported types: png, jpeg, mp4, webm, mp3, wav, pdf. Response: `{ data: { asset_id, url, mime_type, size_bytes } }`. Supports `Idempotency-Key` header for safe retries within 24 hours. | https://developers.heygen.com/reference/upload-asset |

### 2.3 POST /v3/videos — image-to-video (Avatar IV)

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| Endpoint: `POST https://api.heygen.com/v3/videos` | **CONFIRMED** | Confirmed. | https://developers.heygen.com/image-to-video |
| `type: "image"` for image-to-video mode | **CONFIRMED** | `type: "image"` is mutually exclusive with `type: "avatar"`. | https://developers.heygen.com/image-to-video |
| `image: { type: "asset_id" }` (from POST /v3/assets) | **CONFIRMED** | `image.type` accepts `"asset_id"` (from upload) or `"url"` (public URL). The plan's default of uploading bytes first via `/v3/assets` then referencing `asset_id` is the correct and recommended approach (no URL-lifetime dependency). | https://developers.heygen.com/image-to-video |
| `engine: { type: "avatar_iv" }` parameter | **CHANGED** | For the `type: "image"` endpoint, the `engine.avatar_iv` field and `motion_prompt` / `expressiveness` are **not documented**. Those parameters are only present in the `type: "avatar"` (Digital Twin) endpoint, where Avatar IV is the default engine. For image-to-video, the engine selection is implicit (no `engine` field in the request schema). **Action required:** plan references `engine:{type:"avatar_iv"}` in the image-to-video payload — this appears to be incorrect. Remove `engine` from the `type:image` payload. The Avatar IV technology is still used under the hood, but the parameter is not exposed. | https://developers.heygen.com/image-to-video |
| `script` + `voice_id` for TTS audio | **CONFIRMED** | `script` (text) + `voice_id` (from GET /v3/voices) is a valid audio source for image-to-video. Mutually exclusive with `audio_url`. | https://developers.heygen.com/image-to-video |
| `audio_url` as alternative audio source | **CONFIRMED** | `audio_url` is a valid alternative to `script`/`voice_id`, mutually exclusive. | https://developers.heygen.com/image-to-video |
| `motion_prompt` parameter | **CHANGED** | `motion_prompt` is **not documented** for the `type: "image"` endpoint. It appears only in the `type: "avatar"` Digital Twin endpoint. **Action required:** remove `motion_prompt` from the image-to-video payload or verify with HeyGen support that it is accepted silently. | https://developers.heygen.com/image-to-video |
| `resolution` parameter | **CONFIRMED** | Accepted values: `"4k"`, `"1080p"`, `"720p"`. | https://developers.heygen.com/image-to-video |
| `aspect_ratio` parameter | **CONFIRMED** | Accepted values: `"auto"`, `"16:9"`, `"9:16"`, `"4:5"`, `"5:4"`, `"1:1"`. | https://developers.heygen.com/image-to-video |
| `callback_url` for webhook | **CONFIRMED** | `callback_url` is a supported field; HeyGen will POST to this URL when the video is complete. | https://developers.heygen.com/image-to-video |
| `callback_id` to encode `provider_jobs.id` | **CONFIRMED** | `callback_id` is a custom identifier field included in the webhook callback, allowing round-trip correlation. The plan's approach of encoding `provider_jobs.id` in `callback_id` is valid. | https://developers.heygen.com/image-to-video |
| Response: `{ data: { video_id } }` | **CONFIRMED** | Successful response returns `data.video_id` for polling. | https://developers.heygen.com/image-to-video |

### 2.4 GET /v3/videos/{video_id} — status polling

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| Endpoint: `GET https://api.heygen.com/v3/videos/{video_id}` | **CONFIRMED** | Confirmed. | https://developers.heygen.com/reference/get-video |
| Status values: `pending → processing → completed \| failed` | **CONFIRMED** | The schema defines exactly four status values: `pending`, `processing`, `completed`, `failed`. (Note: "waiting" appeared in one search result but is **not in the official response schema** — treat as an alias for `pending` if encountered.) | https://developers.heygen.com/reference/get-video |
| Response includes `video_url` on completion | **CONFIRMED** | Response: `{ data: { id, status, video_url, thumbnail_url, duration, failure_code, failure_message, ... } }`. `video_url` is a presigned download URL, present when `status === "completed"`. | https://developers.heygen.com/reference/get-video |

### 2.5 GET /v3/voices — voice listing

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `GET /v3/voices` endpoint exists | **CONFIRMED** | Endpoint: `GET https://api.heygen.com/v3/voices`. Query params: `type` (public/private), `engine` (e.g. "starfish"), `language`, `gender`, `limit` (1-100, default 20), `token` (pagination cursor). Response: `{ data: [{ voice_id, name, language, gender, preview_audio_url, support_pause, support_locale, type }], has_more, next_token }`. | https://developers.heygen.com/reference/list-voices |

### 2.6 v1/v2 sunset timeline

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| v1/v2 sunset date | **CONFIRMED** | Legacy v1/v2 endpoints remain fully operational and supported through **October 31, 2026**. After that date they may be deprecated. All new development should target v3. | https://developers.heygen.com/docs/quick-start |

---

## 3. Remotion — Render and Editor

### 3.1 Current version

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| Remotion pinned version | **CHANGED** | The workspace is pinned to **v4.0.491** as of July 2026. All Remotion packages must stay on the same exact version, and its bundled custom license must be reviewed before commercial use. | https://github.com/remotion-dev/remotion/releases |

### 3.2 Company License

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| Company License required at 4+ employees | **CONFIRMED** | Free for for-profit organizations with **up to 3 employees**. A company license is required once the organization exceeds 3 employees (i.e., 4+ employees). Contractors **now count** toward team size (added in v5.0 migration). Non-profits and individual developers remain free. | https://www.remotion.dev/docs/license |
| No revenue threshold — size-based only | **CONFIRMED** | The threshold is purely headcount (3-employee free tier), not revenue. No revenue threshold mentioned. | https://www.remotion.dev/docs/license |
| v5 mandatory telemetry for render-based use | **CONFIRMED** | "From Remotion 5.0, telemetry reporting using the `licenseKey` option is **mandatory** for Remotion for Automators" (render-based licensing). "Remotion for Creators" (seat-based, low-volume internal use) telemetry remains optional. The plan's use case (serving generated videos to end users) is **Automators** — mandatory telemetry applies when on v5. On v4, telemetry is voluntary. | https://www.remotion.dev/docs/licensing |
| Company License is a P0 compliance gate before commercial launch | **CONFIRMED** | Remotion states this directly. Must acquire a license from remotion.pro before commercial deployment if the org is 4+ employees. | https://www.remotion.dev/docs/license |

### 3.3 Licensing model terminology

| Term | Meaning |
|---|---|
| **Remotion for Creators** | Seat-based. Low-volume rendering within and for your own company, not serving personalized videos to end users. Telemetry optional even on v5. $100/month minimum spend. |
| **Remotion for Automators** | Render-based. Serving generated/personalized videos to end users at scale. **Mandatory telemetry from v5.** Enterprise tier: $500/month minimum spend + per-render pricing. |

The AI video SaaS described in the plan is **Automators** category.

### 3.4 Bun / `remotionb`

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `@remotion/renderer` runs under Bun via `remotionb` binary | **CONFIRMED** | `remotionb` CLI command exists for running Remotion with Bun. Supported from Bun v1.0.3+. `bun create video` scaffolds with `remotionb`. Known constraint: `lazyComponent` prop is automatically disabled under Bun; SSR scripts may not auto-quit after completion. | https://www.remotion.dev/docs/bun |
| Bun minimum version: 1.1.3 | **CONFIRMED** | Remotion v5.0 requires minimum Bun 1.1.3. | https://www.remotion.dev/docs/5-0-migration |

### 3.5 OffthreadVideo

| Claim in plan | Status | Finding | Source |
|---|---|---|---|
| `OffthreadVideo` works with `@remotion/renderer` (local) | **CONFIRMED** | `<OffthreadVideo>` is compatible with server-side rendering via `@remotion/renderer`. Uses FFmpeg for frame extraction (not browser APIs), making it suitable for SSR. | https://www.remotion.dev/docs/offthreadvideo |
| `OffthreadVideo` NOT compatible with `@remotion/web-renderer` | **CONFIRMED** | Explicitly documented: "this component is not supported in `@remotion/web-renderer`." This validates the plan's elimination of the web-renderer export path. | https://www.remotion.dev/docs/offthreadvideo |
| Bun compatibility for `OffthreadVideo` | **CONFIRMED** | Compatibility table shows `<OffthreadVideo>` works with Bun (no noted constraints in the OffthreadVideo docs). | https://www.remotion.dev/docs/offthreadvideo |

### 3.6 Other packages

| Package | Status | Note |
|---|---|---|
| `@remotion/player` | **CONFIRMED** | Current, standard package. `<Player>` component + `PlayerRef` for `seekTo`/`play`/`pause`. |
| `@remotion/lambda` | **CONFIRMED** | Exists as the production Lambda-based render swap. Not verified in depth but well-documented. |
| `@remotion/transitions` | **CONFIRMED** | Referenced in docs. |
| `<Series>` / `<Series.Sequence>` | **CONFIRMED** | Standard Remotion composition primitives. |
| `<Audio>` | **CONFIRMED** | Standard component. |

---

## 4. Action Items for Implementers

The following plan claims require correction before provider coding begins:

1. **HeyGen `engine: { type: "avatar_iv" }` in image-to-video payload** — CHANGED. For `type: "image"` (image-to-video), the `engine` field is not in the documented schema. The Avatar IV engine is implicitly used. **Remove `engine` from the `type:image` payload in `HeyGenAnimationProvider`.**

2. **HeyGen `motion_prompt` in image-to-video payload** — CHANGED. `motion_prompt` is not documented for `type: "image"`. Only exists for `type: "avatar"` (Digital Twin). **Either remove `motion_prompt` from the image-to-video payload or explicitly confirm with HeyGen API support that it is accepted.**

3. **`personGeneration` on `gemini-3.1-flash-image`** — UNVERIFIED. Confirmed for Imagen 4 but not for the Gemini-native `generateContent` path. **Before P1 coding: verify whether `personGeneration` is a valid `generationConfig` key for `gemini-3.1-flash-image`, or omit it.**

4. **Remotion version pinning** — The workspace is pinned to v4.0.491. Keep every Remotion package aligned and re-review the bundled license before any upgrade.

5. **`imagen-4.0-generate-001` for "independent shots" mode** — CONFIRMED. Model is valid and stable. `personGeneration: "allow_adult"` is confirmed for this model. Note: `allow_all` option is geo-blocked in EU/UK/CH/MENA.

---

## 5. Sources Summary

- https://ai.google.dev/gemini-api/docs/models
- https://ai.google.dev/gemini-api/docs/image-generation
- https://ai.google.dev/gemini-api/docs/imagen
- https://www.npmjs.com/package/@google/genai
- https://developers.heygen.com/docs/quick-start
- https://developers.heygen.com/reference/upload-asset
- https://developers.heygen.com/image-to-video
- https://developers.heygen.com/reference/get-video
- https://developers.heygen.com/reference/list-voices
- https://www.remotion.dev/docs/license
- https://www.remotion.dev/docs/licensing
- https://www.remotion.dev/docs/5-0-migration
- https://www.remotion.dev/docs/bun
- https://www.remotion.dev/docs/offthreadvideo
- https://github.com/remotion-dev/remotion/releases
